{
  "name": "@eliteflow/installer",
  "version": "0.1.0",
  "private": true,
  "description": "EliteFlow Nebula GUI installer (Electron) — ADR-0002 D4.",
  "author": "EliteFlow Nebula",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder --win nsis",
    "test": "tsx test/core.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
